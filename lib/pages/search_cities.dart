import 'package:flutter/material.dart';

class SearchCities extends StatelessWidget {
  const SearchCities({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Realizar busquedas"),);
  }
}